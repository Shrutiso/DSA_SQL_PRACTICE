--Group by queries
select dept, count(emp_name) as Total_employees from Employee
group by dept;
select dept ,  avg(emp_salary) as Avg_salary_dept_wise from Employee
group by dept;
select  max(emp_salary)from Employee
group by dept;
